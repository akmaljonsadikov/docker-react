language: java
sudo: required 
services:
  - docker

before_install:
  - docker build -t akmaljonsadikov/docker-react -f Dockerfile.dev .  

script:
  - docker run -e CI=true bf73f3ae3731 npm run  test -- --coverage