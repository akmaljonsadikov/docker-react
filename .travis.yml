language: node_js
node_js:
  - 7
os: osx
sudo: required 

services:
  - docker

before_install:
  - docker build -e CI=true -t akmaljonsadikov/docker-react -f Dockerfile.dev .  

script:
  - docker run -e CI=true akmaljonsadikov/docker-react npm run test 